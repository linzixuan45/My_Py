#创建dataframe对象
# 创建DataFrame对象需要三个参数
#
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

data = np.random.randn(60,101,(5,3))
index = ['math', 'chinese', 'english', 'physic', 'social']
columns
df = pd.DataFrame(data,index=,columns=)